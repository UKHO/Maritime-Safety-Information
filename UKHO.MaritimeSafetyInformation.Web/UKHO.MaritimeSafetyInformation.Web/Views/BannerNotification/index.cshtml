@using UKHO.MaritimeSafetyInformation.Common.Configuration

<!--Phase Banner-->
 @if (!string.IsNullOrEmpty(Global.BannerNotificationMessage))
 {
    <div class="phase-banner">
        <div class="container-fluid">
            <div class="row">
                <div class="col-sm-12">
                    <P>
                        @*<span class="alpha-caption">Beta</span>*@
                        @Html.Raw(Global.BannerNotificationMessage)
                    </P>
                </div>
            </div>
        </div>
    </div>
}

